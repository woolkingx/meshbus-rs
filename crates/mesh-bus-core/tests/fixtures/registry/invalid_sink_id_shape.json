{
  "name": "invalid_sink_id_shape",
  "params": { "layer": "registry", "exercise": "verify" },
  "input": {
    "json": {
      "sources": [{ "id": "src", "kind": "application/source", "initial_writes": [] }],
      "sinks": [{ "id": "bad/id", "kind": "stream_egress" }],
      "hooks": [
        { "id": "allow", "kind": "Net", "allowed_namespaces": [], "reads": [], "writes": [], "may_terminate": true, "may_jump": false, "may_jump_to": [], "may_accept_to": ["bad/id"], "side_effect_only": false }
      ],
      "pipelines": [{ "id": "main", "hooks": ["allow"] }],
      "wirings": [{ "source": "src", "pipeline": "main" }]
    }
  },
  "expect": { "ok": false, "error_contains": "invalid SinkId" }
}
