id: binary_socks5_auth_user_pass_rejects_wrong_password
owner: mesh-bus-bin.e2e
kind: service-composition
case: e2e.roundtrip
schema_ref: schemas/test-runtime.schema.json
input:
  probe:
    kind: socks5_auth_reject
    username: alice
    password: wrong-pw
  yaml: |
    ingresses:
      - kind: Socks5
        listen: "{{TCP_LISTEN}}"
        auth:
          users:
            - name: alice
              password: s3cret
    egresses:
      - kind: Tcp
        id: direct
        timeout_ms: 1000
expect:
  ok: true
observations: []
tags: []
